# Small-shop-pos

Small web based POS system to log sales and track inventory.

## Setup

Copy `.env.example` to `.env`, then replace the placeholder values with your Supabase project URL and publishable key before starting the app.
